# 开工指南（2026-09-22）

本地演示已删除，所有业务页面统一连接个人云环境。实际经营按一家门店，保留客户、店员、管理员及跑腿业务。

1. 微信开发者工具重新编译，进入注册/登录页。
2. 核对 AppID wxba87853fc5deca39、云环境 cloudbase-d9g2aibtdf3e4d6bc。
3. 开发版/体验版保留测试手机号登录，前端 devLogin=true、云函数 DEV_LOGIN=1。使用已登记的测试号码；账户切换以已部署的身份逻辑为准。
4. 店员角色由管理员配置，管理员权限由环境拥有者核验后设置。此清理不删除或迁移云端账号与业务数据。
5. 修改代码后运行 npm test、npm run check、npm run env:check:dev。前后端变更应配套部署，不再运行 demo:sync。
6. 正式发布前关闭 devLogin、删除云端 DEV_LOGIN，填写公司 AppID/环境与隐私信息并执行 env:check:prod。

旧设备上的演示缓存不再读取，无需清空整个微信缓存。媒体选择只在本机预览，保存时上传云存储；实际权限仍需云端验证。
