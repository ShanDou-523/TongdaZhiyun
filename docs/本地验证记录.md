# 本地验证记录

## 当前版本：2026-09-21 整合版

- `npm test`：73 项通过，0 失败。
- `npm run check`：88 个文件，JavaScript 语法、JSON、9 组页面与 tab 路由检查通过；演示业务副本同步检查通过。
- 本机微信开发者工具 `wcc.exe`：9 个页面及1个组件，共10个 WXML 文件编译通过。
- 本机微信开发者工具 `wcsc.exe`：全局样式、9页及1个组件，共11个 WXSS 文件编译通过。

宿舍号增量验证：注册/登录持久化、资料修改及清空、旧用户与旧客户端兼容、门店可见、本地演示均通过；6个改动页面 WXML 编译通过。

本次新增注册位置字段、正式版开发登录禁用、昵称输入绑定、演示服务/广告增删改查、素材上传重试复用、云适配器内容检测异常等回归。

自动测试覆盖原有身份与业务规则、页面轮询、通知已读，以及五个演示身份、模拟注册、双向聊天、客户隐私隔离、管理操作、数据持久化/重置、正式版禁用演示和真实/演示会话隔离。

演示复用云端业务规则的生成副本，数据写入模拟存储。测试不会连接或写入真实云数据库。

尚未执行：开发者工具模拟器视觉验收、真实微信手机授权、云端事务与安全规则联调、多设备聊天、真实入口码生成/扫码、云函数部署、上传和发布。本地编译与模拟测试通过不代表上述项目完成。

已填写云环境 ID，但共享配置与本机私有配置的 AppID 不一致。真实联调前核对开发者工具实际身份、公众平台声明和云环境关联。

云端媒体上传的管理员专属存储授权尚待实现与验证；暂不对外宣称媒体功能已完成云端验收。

下一步按《本地演示与功能验收》体验现有功能，再按《开发推进计划》补齐洗护与家政两条完整业务流程。

环境检查：个人环境检查完成并提示AppID差异和演示模式；公司生产检查应因配置缺失而返回1。这些检查只读本地，不验证云端状态。

## 上传前配置同步

合入远程 836b359 后，共享 AppID 已与个人开发目标统一为 wxba87853fc5deca39。此前记录的共享/私有 AppID 差异已消除；实际云端能力与部署仍待联调。

## 2026-09-21 订单/跑腿修复后合并

基于远程 9bd13c5。86项测试通过；静态检查105个文件、13组页面通过；14个WXML、15个WXSS通过原生编译。新增覆盖：跑腿未送达拒绝门店及管理员接单、自送拒绝跑腿抢单、演示/云端照片分流、正式版隐藏测试占位、带本地图片的申请及履约演示。

仅本地模拟与编译，未部署、未做真实证件上传、云数据库及支付验收。此节为最新结果，前文为历史记录。

## 2026-09-22 广告修复

90项测试通过，105文件静态检查通过。新增覆盖上传中切换广告及表单修改、失败后沿用请求编号、并发创建去重、内容不一致拒绝、删除后重放不复活、服务/广告单侧失败。未修改模板样式，因此未重复原生编译；云端与真机弱网验收待部署后执行。

## 2026-09-22 删除本地演示后的最新验证

71项测试通过；90文件静态检查、12个WXML和13个WXSS编译通过。测试数减少是删除了演示专用测试，云端业务、广告修复和开发登录测试保留。新增旧演示缓存不影响云端请求及直接进入登录页的检查。没有部署云端或删除设备/云数据库数据。
