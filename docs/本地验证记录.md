# 本地验证记录

本次交付结果：

- `node --test tests/*.test.js`：20项通过，0失败。
- `node scripts/check.js`：所有 JavaScript 语法、JSON 格式、8组页面与 tab 路由检查通过。
- 本机微信开发者工具自带 `wcc.exe`：8个 WXML 页面编译通过。
- 本机微信开发者工具自带 `wcsc.exe`：全局及8页共9个 WXSS 文件编译通过。
- `npm install --package-lock-only --ignore-scripts --no-audit --no-fund`：云函数依赖锁文件生成成功；未运行依赖安装脚本。

自动测试使用内存仓库，不会连接或写入真实云数据库。页面测试模拟微信 API，验证拒绝授权、注册信息校验、登录失败恢复、消息重试和二维码参数解析。

未执行：开发者工具模拟器视觉验收、真实微信手机授权、云端事务与安全规则联调、实际二维码生成/扫码、云函数部署、上传、发布。缺少真实 AppID 与云环境 ID，不能将本地编译通过等同于已上线可用。

后续按《验收清单》在测试云环境和真机完成这些验证。
