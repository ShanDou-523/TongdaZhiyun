# 更新日志

## 未发布 — 2026-09-22 广告保存与首页加载修复

- 广告保存使用点击时的完整快照；上传/保存期间阻止切换广告、清空、修改字段和移除素材，过期素材选择回调不会覆盖新表单。
- 新建广告使用表单内稳定 requestId，网络失败重试沿用；后端通过 auditLogs 中的事务记录去重，并核对内容。重复或并发请求只创建一条广告；删除后重放旧请求不会重新创建。清空或切换表单表示新操作，旧客户端无 requestId 时保持兼容。
- 服务和广告独立并行加载，各自成功即可展示；失败保留已有列表并显示对应错误，旧加载结果不覆盖新一轮刷新。
- 本地演示同步相同业务规则。90项测试及105文件静态检查通过。
- 本次不改登录逻辑、不部署或推送。云端去重需前端与 api 一起更新；无需新增集合，去重依赖审计记录保留。会话昵称同步问题仍待处理。


## 未发布 — 2026-09-21 订单与跑腿版本合并修复

- 合入远程 9bd13c5 的订单、跑腿、客户换店和账户搜索。
- 修复跑腿订单在 submitted 状态可被门店直接接单的问题；只有 delivered 才能进入服务中，门店和管理员使用相同限制。自送订单不能由跑腿抢单。
- 新增下单和跑腿申请页面使用实际 demoMode 分流照片：本地演示不上传云存储，演示后端保留本地临时图片；真实云端仍仅接受云文件，开发占位符受服务端 DEV_LOGIN 控制。
- 补上演示标识、初始化期间操作保护和正式版云端跳过证件按钮限制；演示占位图显示文字提示。
- 验证：86 项自动测试、105 个文件静态检查、14 个 WXML 与15 个 WXSS 编译通过。
- 本次未部署云环境。新增 orders/settings 集合及索引仍需部署核验，真实媒体授权和支付结算未完成。先前广告保存、重复新增、首页加载和会话昵称问题不在本次修复范围。


## 未发布 — 2026-09-21 宿舍号录入

- 注册、我的资料及演示注册在园区下方增加选填宿舍号，最多20字，支持楼栋和房间组合。
- 接通保存、重新加载及门店/管理员客户列表展示；旧用户无字段时显示空白，旧客户端未提交字段时保留已有宿舍号，可主动清空。
- 验证：73项测试通过，静态检查及6个改动页面 WXML 编译通过。
- 同步本地演示业务规则、资料收集说明；云端使用需部署更新后的 api 云函数，无需批量迁移旧用户。

## 未发布 — 2026-09-21 版本整合

- 整合远程 d15166c 与本地 e4dcc03；保留消息分页、轮询生命周期、按员工及通知已读、隐私错误提示和环境检查。
- 合入十一园区、青教公寓、“其它”位置字段，管理员服务增删改查、首页广告及媒体预览、个人云端开发登录和文字内容检测。
- 修复“其它”注册漏传位置；补齐本地演示相同字段；修复资料昵称输入缺少字段绑定。
- 统一一家门店、五个演示身份，复用云端规则；移除远程重复 mock 实现，补齐演示仓库删除能力和广告管理。
- 更正同一微信换手机号可切换账号的误导提示；开发登录仅开发/体验版显示，服务端仍由 DEV_LOGIN 控制。个人测试号码不显示“已验证”。
- 内容检测异常和缺失结果不再静默放行；返回可重试错误。实际平台能力仍待云端验证。
- 广告上传成功但保存失败时保留 fileID 供重试；补充视频时长检查；删除/替换广告不物理删除素材，避免误删共用文件，后续需核对并清理无引用素材。
- 更新开工、开发、环境切换说明，保留公司环境隔离边界。没有部署云资源、修改 AppID 或实现订单支付。云端媒体上传授权仍待完成。

## 未发布 — 2026-09-19

### 新增

- 保存现有源码Git基线；核对远程仓库尚未包含交接说明中的升级。
- 增加个人开发/公司生产目标清单及只读环境检查，生产参数缺失或测试开关未关时返回失败；实际云端部署与权限仍需核验。
- 补充源码整合边界、测试数据隔离及公司上线切换清单。

- 本地演示入口：客户、一家门店员工和管理员五个身份，无需手机号或云端部署。
- 模拟新客户注册、角色切换、聊天、门店/账户管理、持久化和重置；提供分页与历史消息样本。
- 复用服务端业务规则生成本地副本，增加同步检查；正式版禁用演示，演示会话与真实会话独立。
- 新增现有功能验收指引与洗护、家政完整开发推进计划。

### 调整

- 按实际经营改为一家演示门店：两名客户、两名店员和管理员共五个身份。改用 v2 演示存储空间，重新编译后显示新的单店数据，旧演示数据保留但不再读取。

### 修复

- 手机号注册区分隐私范围未声明（errno:112）、隐私接口停用和隐私授权拒绝；缺少有效 code 时不调用后端。补充公众平台配置排查说明。本次代码调整只改善错误提示，消除 errno:112 仍需后台补齐手机号声明。

- 消息列表每 15 秒刷新已加载的全部分页，保留浏览范围，不再将 40 条等已加载结果重置为第一页 20 条；合并时按会话 ID 去重，刷新失败保留原列表。
- 消息列表、门店通知和聊天轮询增加页面生命周期代次校验。页面隐藏、重新进入或卸载后，旧请求不能覆盖新页面结果或重新启动轮询。
- 门店通知改为按“员工 + 通知”保存已读。按钮只提交已显示的未读通知 ID，不再通过时间截止点误标更早或同一毫秒到达的未显示通知。
- 通知接口在事务内校验员工身份和门店归属；每次最多标记 20 条，重复提交保持幂等。
- 聊天页仅在阅读序号推进后提交已读；失败后下次刷新重试，隐藏期间返回的消息不提交已读。

### 配置与文档

- 云环境已配置为 `cloudbase-d9g2aibtdf3e4d6bc`。
- README 同步现有 AppID、云环境配置状态；补充已读数据格式和升级说明。
- 运营主体、隐私联系方式仍待提供。

### 验证

- 本地回归测试：55 项全部通过（原有 20 项，累计新增 35 项）。
- 静态检查：83 个文件、9 组页面及路由检查通过。
- 微信编译器验证：10 个 WXML 文件、11 个 WXSS 文件全部通过（含演示组件）。

- 新增消息分页保留、快速切页、延迟响应、刷新失败、已读重试等页面回归测试。
- 新增超过 20 条通知、同时间戳通知、员工隔离、跨门店拒绝及旧数据兼容测试。
- 本地验证使用模拟微信 API 和内存数据库，不代表微信真机或云端验收完成。

### 升级注意

- 本版需要同时更新小程序端和 `api` 云函数；旧客户端按时间提交通知已读的请求会被拒绝，避免继续误标。
- 继续使用现有 `notificationReads` 集合，无需增加集合或索引。新记录按员工和通知的哈希 ID 存储；兼容读取旧版用户级 `readAt`，不再推进旧时间戳。
- 旧版已经误标的记录无法从时间戳推断真实阅读情况，本版不会自动恢复。
- 消息列表保留 offset 分页；跨页查询期间若会话实时重排，仍可能短暂遗漏，后续刷新重新读取。此版未引入服务端快照分页。

## 未发布 — 同步团队开发 AppID

- 合入远程 836b359：共享项目 AppID 更新为 wxba87853fc5deca39，与当前个人开发环境目标一致；公司正式 AppID 仍待提供。
